/* 
Helper functions for dynamics
*/

#include "include/p_cats.h"

// Function to calculate magnetic force on a point
Force magnetic_force(Point pa, Charge qa, Velocity va, Point pb, Charge qb, Velocity vb) {
    double mu0 = 4 * M_PI * 1e-7;
    
    return Force(x, y, z)
}